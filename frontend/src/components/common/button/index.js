import React from 'react';
import { Box, Button } from '@material-ui/core'


function button() {
    return (
        <Box>
            <Button id="login-btn" type="submit" size="large" variant="contained">Login</Button>
        </Box>
    );
}

export default index;